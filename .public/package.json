{
  "name": "@barfinex/config",
  "version": "0.1.0",
  "description": "Configuration management utilities with type-safe env loading and schema validation.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.app.json",
    "lint": "eslint 'src/**/*.ts'"
  },
  "license": "Apache-2.0"
}